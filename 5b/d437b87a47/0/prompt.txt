can you commit it all, and push it - `.env.local` should be excluded automatically by `.gitignore` with `.env.example` excluded , add the script as well , use commit msg "feat: Infrafund backend next migration - env template"

Then give me a list what values it still need to populate to make it work, grouped by feature (i.e. sending email) sorted by priority , openfort login and database likely at the top , recaptcha , mail more towards the end with less priority