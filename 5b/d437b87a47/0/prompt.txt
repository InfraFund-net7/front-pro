We have done most of the migration from a Go based backend to this single Next.js repo as 
 described in @docs-dev/tasks/task-100-nextjs-migration-plan.md 
 - Final step should be Phase 10 and whatever remaining task there are left to wrap this up.
 - Review what has been done so far
 - Make sure we have a clean , lean openfort integration, refer to `openfort` `SKILL.md` for 
 documentation and guidance. 
 There is also a demo app code for reference : 
 https://github.com/openfort-xyz/openfort-js/tree/main/examples/apps/auth-sample 
 
 - Make sure we do not rely on the backend for anything, and do not have any code remaining 
 which was only there to support the backend 
 - test as far as you can do, we may need a separate manual + browser based test run to finally 
 - verify if everything is working as expected 
 - once everything is green, write a new file 
 `docs-dev/release-notes/v1.0.10-backend-next-migration-openfort-simplification.md` , update 
 the migration plan , commit and push as "feat: Infrafund backend next migration - phase 10"

---

this is how `.env.local` currently looks like. so it should have everything needed to run it locally ? 
```
# =============================================================================
# Openfort — get these from: https://dashboard.openfort.io
# Or from the CLI: cat ~/.config/openfort/credentials
# =============================================================================

# Client-side (exposed to browser)
REDACTED
NEXT_PUBLIC_SHIELD_API_KEY=8************************************

# Embedded wallet keys (Shield)
REDACTED

# Server-side only (never exposed to browser)
# Get these by running: openfort embedded-wallet setup
SHIELD_SECRET_KEY=****************************************
SHIELD_ENCRYPTION_SHARE=**********************************


# Gas sponsorship policy ID (optional, from dashboard > Policies > Fee Sponsorship)
NEXT_PUBLIC_POLICY_ID=

# WalletConnect (now cloud.reown.com) project ID (optional, enables external wallets like MetaMask)
REDACTED

# Backend API (your project's backend, if any)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1
```
can i remove `NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/v1` ?

---

a few env vars sound similar to the ones you mentioned as missing
for local testing we have a local postgresql or supabase install which we can use - you pick one, we might just use the one which the old `../backpro` repo used , although i do not theink that we even have more the 1 record in , but the db might already exist.
tell me how and where to get the other env vars

---

i copied the `.env` file from the old `../backpro` repo which looks like a very comprehensive setup which may serve us as template to have all env vars in a structured way.
in the 'Auth Configuration' section I have revoved parts of the secret keys which I fill in back later.
That sections also used a good naming scheme with all env vars starting with `INFRA_AUTH_'
I would not go that far to demand to go through our whole application and change it all, I would rather prefer to use the established/standard names at least for openfprt what they use in the code examples to stay consistent. 
Otherwise we may keep those names ?
Not sure, on one side that naming scheme look very structured, on the other side its good to use the same naming conventions as commonly used (i.e. by openfort), to avoid any programmatic or mental mapping, with potential erros. WDYT ?

We also have `.env` and `.env.example` and I would like to consolidate that into a clean setup for up and a example version