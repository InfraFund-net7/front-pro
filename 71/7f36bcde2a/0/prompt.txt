what is the difference between @public/models/digital-twin/wind-turbine/Wind_Turbine_2.gltf and @public/models/digital-twin/wind-turbine/Wind_Turbine_3.gltf
A simple git diff show "everything is different" although we just separated the model 2 into sub-component and stored it as model 3, and i would like to know what was done to achieve that.

---

what properties to the individual components have, all the same, which ones?
how can I attach addditional properties to each component ?
maybe it would be good to have the `gltf` file in a `json`-type aware database like postgresql to be able to add properties, and set their values , then read the `gltf` file from there for rendering ?
On the page `/projects/1/digital-twin` we have the model rendered , how can we implement that a user can click on a component, we get the metadata, and display it either within the view/render canvas or in a ,simple text to start with, component to the side of the viewport ?

---

As we want to have a flexible implementation which suits whatever model we have to work with, whatever metadata it has attched, I would suggest to not try to map the metadata to variables of fixed type, but just get name-value pairs and display them as plain text. WDYT ?

---

i would suggest we have a separate display component , maybe taking 25% off the right side of the viewport to displat our metadata. The "Ground" and "Light" controls could get into there as the top 2 components as well, with the free text area for the metadata below.
Should we have a narrow screen (mobile phone) that complete component should just go under the viewport component.
ok, start with the implementation

---

- that's great. However, the list of properties is so long that the component becomes very high. Let's limit its size and have a scroll bar.
- what is "Inspection" below the name of the component , lets remove it
- put the name value pair into one line to save vertical space
- what does the (Clear) button is about ?
- for the case that we have a wide screen and the metadata component is to the right side of the viewport , can we make the separation moveable, starting at a 75:25 ratio
- try to save some space vertically for the individual metadata name-value pairs

---

- i can move the divider to the right, but not back to the left
- start with 66:34 devider position
- make the font for the name-value pair test a bit larger again, i think the size we had before was actually good, do not increase spacing between line, could be even a bit less
- the scroll bar does not match our theme, it is white while the whole page is a dark theme

---

looks good now, but still I can only move the devider to the right , making the metadata component smaller , but I can not move the devider back to the left side to increase the space for the metadata box again, and in turn make the viewport narrower again

---

works much better now
- move the checkboxes before the labels in the "View Controls" component
- i think there is still a limitation of the ratio? Looks like ~55:45 to me, make it 50:50
- the layout is not really responsive. Is that an overall problem of the page that we have not planned for it ? At least the view control - metadata component should go under the viewport if the window becomes too narrow.

---

Works great !
One more thing. The header with the title of the project has a large font and is bing squeezed on a narrow screen into 4 lines, overwrting what is close. Suggestion :
- put all top right component into one , the project title into another box component. when screen is narrow move the project name below the other component (holding various icons , avator , ...)
- Also make the font for the project title about 2/3 the size. Ideally we would have the page title design specified once across the app, and just reuse it so that it is consistent, also we may reuse the 2 component split for the page title (in this case project name) and the icons for all other pages

---

- I wanted the page title / project name to move below the icons on a narrow screen , that is not happening
- make the space above (Project ID ) towards title   the same as the space below to the viewport
- what do we do with the menu on a narrow mobile screen, hide it and just have a menu icon to slide it in ? I am happy for any default established standard solution

---

menu slide in works, just not the header component stacking. In case we switch to narrow mobile, the single line header should become 2 like :
---
[ menu button (left aligned) / headphone, bell, openfort button, avator circle (right aligned) ]
[ Title/Projectname (left aligned) ]
---

---

looks great 
-  write release notes file `docs-dev/tasks/v1.2.7-gltf-get-metadata-from-model-click.md` put it into `docs-dev/release-notes`
- rename our taskfile to v1.2.7 , and i think we can mark phase 1 as completed
- update version in package.json 
- commit
- tag with v1.2.7
- push it

---

looks great, just one minor thing. the view in the glTF viewer always start with looking at an angle from the bottom, can we cange the start default view at angle from above the ground plane , like 45/2 degree. Just change that, i will monitor the rendering and give feedback

---

that did not change anything, maybe you have to closer inspect the model coordinates and ground plane position. I know that this model had be default the ground plane at the upper tip of the model, so we create (afaik) a bounding box of the model and then move the ground plane to the lower end off the modell.
However in an case once we have the coordinates of the ground plane we should be able to determine what "above the groundplane" means (actually the side of the ground plane where the model is

---

you have to use `headroom-azure` to access the proxy we are currently using, ok continue ...