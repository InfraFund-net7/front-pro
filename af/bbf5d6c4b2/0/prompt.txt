I had a bad regression after some code clean up and upgrade of some next/react libs, although likely the 
 code cleanup was the issue. 
 task was : @docs-dev/tasks/task-113-fix-disconnect-logout.md 
 It looked like that it was fixed , release notes here : 
 @docs-dev/release-notes/v1.1.3-fix-disconnect-logout.md 
 
 Now I implemneted some additional detail pages for the project, see `docs-dev/release-notes/v1.2.0-digital-twin-pages-with-glTF-3d-model.md` and `docs-dev/release-notes/v1.2.1-digital-twin-rendering-polish.md` which may not have had any influence, but I may just have noticed only then that when I refres

---

ignore the error, just work on the task i described initially

---

that did not help. 
- I stopped the dev server, cleared `.next` directory, restarted server, refreshed the browser.
- Then I logged in again (as an existing user), ended up on the `home` page
- clicked the refresh button , and again i went through the whole 5 step login process to just refresh the current page

Did you read @docs-dev/tasks/task-113-fix-disconnect-logout.md                                                    
and   @docs-dev/release-notes/v1.1.3-fix-disconnect-logout.md ?
We spent a lot of time to fix this logout issue, somehow it was more complicated than you would think and expect, so these are the important files for context and I think the solution to our current issue needs likely more investigation and thinking.

---

looks like you got stuck. was there anything left to do ?

---

now the page goes all black for about 10 sec on a page refresh. Almost looks like as if it still does the same login process behind the scenes (which takes a bit more than 10 sec) , just not displays the 5 step process in the popup
We also gathered some knowledge derived from the very time consuming process to fix the logout loop here : @docs-dev/openfort/disconnect-logout-implementation-pattern.md , maybe that has additional information to develop a clean solution

---

why is there even a need to re-connect the openfort embeddet wallet. Does it survive a simple page refresh ?

---

Although this works now for the home page, I tried a page refresh on our new project detail page with #D disgital twin `/projects/1/digital-twin` , when I do a page refresh there, i get redirected to the home page.
The refresh in any case brings up the "loading ..." popup for ~1 sec and in total needs about 2 sec, although that is not ideal I would accept it (for now) as it at least works much better and close to 10x faster than before, but still a page refresh should still stay on the same page.