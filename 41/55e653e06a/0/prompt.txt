whenever we push to develop , a helper tool 'entire' pushed a copy of  the chat session related to the commit to a branch `entire/*` which also gets pushed - this additional oush of a branch is , like the real code branch `develop` , automatically picked up by vercel to start a deployment. However, in case of the `entire/*` branches we do not wat this as they do not contain deployable code, just text, they thus always fail, and also create more problems downstream. We had some efforts to prevent that, but they were all unsuccessful so far. can you investigate how we can fix that.