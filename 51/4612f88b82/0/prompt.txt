change the dashboard (path `/home`)
- "Cornwall Wind turbine Pilot #1" and #2 projects should use the `public/models/digital-twin/wind-turbine/Wind_Turbine 3.gltf` and `.bin` files (#3 already uses it)
- Within the top 4 box components the large font is too large , make it the same size as the heading "My Projects Dashboard" (which looks like about 60% the size)
- change the "Total Funding Raised" amount to "9,500,000" , leading by a pound symbol
- for project #2 and #3 change "Next Milestone: Panel Installation" to "Next Milestone: Rotor Blades Installation"

---

can you center horizontally the content of the 4 boxes

---

for project #1 , we need to make all components of the model visible. Actually , we can derive that from the label - if it is "Operational" then the construction phase has ended and we expect all components to be there.

Make project #2 a copy of project #3 , but we want in this code copy the code as project #3 has a working implementation of selectable "Construction Milestones" which change the visibility of the components of the model. We want to use that as the base to further iterate on while keeping project #2 as a working initial version.

---

above the large 3d render component : 
- if operational show the "Operational" label if project is operational , like project #1 but in a design like "Project ID 1" 
- for in under constructions , show (Construction) in , same style like "Operational" , but in Blue

---

on the dashboard page it is just the wrong way round, make construction blue, operation green

---

- make "Funding" in project "Exeter Community Solar Farm" in the same color as "AI Risk Score"
- for project #1 and #2 , make a line break before "Next Milestone"
- change "View AI-Digital Twin" to "AI Digital Twin" and a link symbol thereafter