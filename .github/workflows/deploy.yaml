name: Deploy to Production Environment
on:
  push:
    branches: ['main']

jobs:
  build:
    name: Build docker image
    runs-on: prod-runner
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Build Docker image
        run: |
          docker build \
            -f ./deployment/Dockerfile \
            --build-arg NEXT_PUBLIC_API_BASE_URL=${{ secrets.PROD_API_BASE_URL }} \
            --build-arg NEXT_PUBLIC_OPENFORT_PUBLISHABLE_KEY=${{ secrets.OPENFORT_PUBLISHABLE_KEY }} \
            --build-arg NEXT_PUBLIC_OPENFORT_SHIELD_PUBLISHABLE_KEY=${{ secrets.OPENFORT_SHIELD_PUBLISHABLE_KEY }} \
            --build-arg NEXT_PUBLIC_OPENFORT_EMBEDDED_CHAIN_ID=${{ secrets.OPENFORT_EMBEDDED_CHAIN_ID }} \
            --build-arg NEXT_PUBLIC_LANDING_URL=${{ secrets.PROD_LANDING_URL }} \
            --build-arg NEXT_PUBLIC_ENVIRONMENT=production \
            -t infra-dashboard:prod .

  deploy:
    name: Deploy to Production Server
    runs-on: prod-runner
    needs: build
    steps:
      - name: Deploy with Docker Compose
        run: docker compose -f ${{ vars.DOCKER_COMPOSE_PATH_PRODUCTION }} up -d dashboard --force-recreate
      - name: Wait for container readiness
        run: |
          echo "Waiting for container to be ready..."
          sleep 5
          docker compose -f ${{ vars.DOCKER_COMPOSE_PATH_PRODUCTION }} ps

      - name: Verify deployment
        run: |
          docker compose -f ${{ vars.DOCKER_COMPOSE_PATH_PRODUCTION }} logs --tail=20 dashboard
