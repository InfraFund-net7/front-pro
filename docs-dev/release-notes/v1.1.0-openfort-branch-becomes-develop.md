<!-- cspell:words Openfort -->

# v1.1.0 Openfort Branch Becomes Develop

The former `openfort` branch is now promoted to `develop`, while the previous `develop` branch is preserved as `develop-particle`.

## Changes

- Created `develop-particle` from the previous `develop` tip to preserve the old line of work.
- Updated `develop` to point at the former `openfort` tip without creating a merge commit or changing existing commit SHAs.
- Kept `openfort` in place as a compatibility branch during the transition.
- Preserved Entire session metadata by leaving commit history intact and keeping `entire/checkpoints/v1` unchanged.
- Switched local development onto `develop` so new commits continue from the promoted branch.

## Validation

- Verified `develop...openfort` was `0 95` before promotion, confirming `openfort` already contained `develop`.
- Verified remote refs after promotion:
  - `develop` -> `a6376ab`
  - `develop-particle` -> `0dd88fa`
  - `openfort` -> `a6376ab`
  - `entire/checkpoints/v1` unchanged
- Confirmed Entire remained enabled and the active session continued after switching from `openfort` to `develop`.
