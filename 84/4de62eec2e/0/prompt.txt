actually create a tag v1.2.0 for this release and push it as well

---

can we now improve the rendering, with the grey background an some default light source which casts a bit of a shadow so that the shape of the model can be seen better. Can we add a ground plane by default. Maybe have a small vertical bar to the right of the 3d view pane to control this, like 
- ground plane on/off
- light on/off
If there are some default control UI elements available and some standard usual things to add , just go with the standard defaults to minimize code and complexity, i have no strikt guidelines, happy to go whatever is common.

---

maybe the grey background overrides it, but i can not see a ground plane. I would have expected that the wind mill would cast some shadown onto the groundplane, but the background is just all grey. Make the ground plane grey instead ?

---

does not make any difference, also the "Ground" check box has no effect

---

ok "Ground" toggle makes a difference but it looks still like a flat backgound in a slightly darker grey. There is also no grid to see on the ground plane, it all just flat grey. Also model is not casting any shadow onto the ground plane.

---

ok, great , I can now see the grid !
Strangely the ground plan is at the very top of the model , so exactly at the tip of the top blade. I assume this is cause be the model coordinates. what would be the best way to fix it ?

---

ok , we are pretty close. looks good. I just have the impression that the shadow id not redering correctly, looks at least cut off. Have a look at `/home/sum/DEV/ETH/InfraFund/3-temp/Screenshot_20260512_171257-gltf-render-broken-shadow.png`

---

actually i see no difference. there is still more space on the plane where the shadow could go , but it is cut off prior. no change from what we had before. The circle around the object is gone for the past 2~3 update , but that is ok, i prefer just the square base plane. Would another redenrer make a difference ?

---

great , now it works. Only additional change : when i toggle "light" or "ground" , the model posistion and size is reset. Can we leave it as is